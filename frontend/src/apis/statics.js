const baseURL = 'http://localhost:3000';
const authRoutes = '/auth'
const usersRoutes = '/users'
const companiesRoutes = '/companies'

module.exports = {baseURL, authRoutes, usersRoutes, companiesRoutes}